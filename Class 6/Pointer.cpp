#include<bits/stdc++.h>
using namespace std;

#define deb(x)  cout<<#x<<" \t"<<x<<endl;

int main()
{
    int *p, *r;
    int q;
    q = 100;
    p = &q;
    deb(p);
    deb(&p);
    deb(*p);


    return 0;
}
